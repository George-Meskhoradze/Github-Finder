import React from 'react';
import ReactDOM from 'react-dom/client';
import GithubFinder from './Gfinder';


ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <GithubFinder />
  </React.StrictMode>
)
